implement error event for Server and Client
Expand CustomError
    Not implemented
    Action can't be performed right now
    Can't set
Reimplement canUsed
Change file structure so that every class gets its own folder
Create guide
    Update readme
    Put examples in guide
    Add screenshots
Move client.window('horse') to horse.openWindow()
    Make Horse class that inherits Entity
    Update Client windowType to different name
Split Color and Text into different packages
Add Client to its own entities object
Convert Client.entities to array where every Entity has EntityId
Expand Client property types
    Create ability to create simple settable properties for Client
        Make bossBars one of them
    Create ability to create dynamic private properties
        Use that for sneaking
        Use that for onGround
Make some boss bar arguments optional
Add position observable to Entity
Make position defaultable in defaultClientProperties
Change loadWorld method to join listener for Client and load listener
Make ability to set position to NaN