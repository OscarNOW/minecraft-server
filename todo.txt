-implement error class/function 
removeAllErrors not correct with types
implement event when error is happening
Implement favicon
Look better at packet order when Client joining and canUsed (use callAfterLogin)
Implement base class which includes EventEmitter, privateSymbols and observers
Use data loader instead of require()
Update tests
Change file structure so that every class gets its own folder
Add screenshots to documentation
Create separate guide
    Update readme
    And put examples in guide
Make docs work with examples again (without examples)
Implement same lazy loading from Text to Color
Make Client.set slot modulo the input. So that if you do `client.slot = 13` it will become 4
Create custom 'Not implemented' error
Create custom 'Action cant be performed right now' error
Check if doc links are still correct